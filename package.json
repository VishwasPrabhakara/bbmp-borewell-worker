{
  "name": "bbmp-borewell-worker",
  "description": "Cloudflare Worker API for the BBMP borewell dashboard.",
  "type": "module",
  "keywords": [
    "bbmp",
    "borewell",
    "cloudflare-workers",
    "neon",
    "gis",
    "dashboard"
  ],
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  }
}
